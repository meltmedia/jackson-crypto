package com.meltmedia.jackson.crypto;

class MavenProperties {
  public static String ARTIFACT_ID = "${project.artifactId}";
  public static String GROUP_ID = "${project.groupId}";
  public static String VERSION = "${project.version}";
}
